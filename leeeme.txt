docker run --rm --name todo-mysql \
-e MYSQL_ROOT_PASSWORD=root \
-e MYSQL_DATABAS=todos \
-v /home/julio/Desktop/bd-todo-backend/data:/var/lib/mysql \
-d mysql:5.7

docker exec -it 5acd5ea85078a118c89b7c737d570 mysql -p 


create database todos;

use todos;

CREATE TABLE IF NOT EXISTS todo (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    description TEXT
);

select * from todo;

INSERT INTO todo (title, description) VALUES  ("julio","juliodes");
INSERT INTO todo (title, description) VALUES  ("juan","juandes");

